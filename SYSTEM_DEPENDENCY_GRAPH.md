# Flowsink — System Dependency Graph

> Generated by full forensic audit (no code modified).
> Legend: **[ACTIVE]** reachable from an entrypoint · **[LEGACY]** old/duplicate · **[ORPHAN]** unreachable · **[BROKEN]** broken reference · **[UNKNOWN]** needs runtime validation · **[EMPTY]** no content

---

## 1. Entrypoints (reachable roots)

| # | Entrypoint | Origin | Status |
|---|-----------|--------|--------|
| E1 | `python -m activity_engine` | `src/activity_engine/__main__.py` | **[ACTIVE]** |
| E2 | `python -m activity_engine.cli` | `src/activity_engine/cli/__main__.py` | **[ACTIVE]** |
| E3 | `activity-engine` console script | `pyproject.toml` `[project.scripts]` → `activity_engine.cli:main` | **[ACTIVE]** |
| E4 | `run_windows.bat` (Windows launcher) | root | **[ACTIVE]** |
| E5 | `uvicorn activity_engine.server:app` | `run_windows.bat`, `src/activity_engine/server.py` | **[ACTIVE]** |
| E6 | Browser extension (MV3 service worker) | `browser-extension/background.js` | **[ACTIVE]** (independent, talks to E5) |
| E7 | `examples/simulate.py` (demo) | root | **[ACTIVE]** (demo only) |
| E8 | `generate_audit_report.py` (tool) | root | **[ACTIVE]** (dev tool) |
| E9 | `install_browser_extension.py` / `uninstall_browser_extension.py` + `.bat` | root | **[ACTIVE]** (install tooling) |

---

## 2. CLI Runtime Chain (E1/E2/E3/E4 default)

```
cli/main.py main()
  ├── configure_logging(logging)
  ├── _cmd_doctor  → platform.screen, adapters.windows.process/network/device_controller,
  │                   adapters.extension.browser_monitor, adapters.mock.*
  ├── _cmd_monitor → config.loader, config.models, engine.facade, adapters.mock.action_executor,
  │                   services.monitoring_service
  │                     └── _build_monitor_set(backend)
  │                           ├── mock:   MockProcessMonitor / MockBrowserMonitor / MockNetworkMonitor
  │                           ├── real:   WindowsProcessMonitor / MockBrowserMonitor / WindowsNetworkMonitor  (Windows only)
  │                           └── extension: MockProcessMonitor / ExtensionBrowserMonitor(api_url) / MockNetworkMonitor
  ├── _cmd_simulate → engine.facade, adapters.mock.action_executor
  └── _cmd_policy_check → policy.loader.PolicyLoader   [BROKEN: default path config/policy.yaml missing]
```

---

## 3. Core Engine Graph

```
engine.facade.ActivityEngine                 [ACTIVE]
  ├── config.loader.load_dev_config / resolve_device_id   [ACTIVE]
  │     └── reads: config/local.yaml  [BROKEN/optional — dir missing]
  ├── policy.loader.load_default_policy                    [ACTIVE] → default_policies.yaml (bundled)
  ├── core.policies.PolicyDocument
  ├── engine.event_engine.EventEngine                      [ACTIVE]
  ├── engine.state_engine.StateEngine                      [ACTIVE]  → domain.session.MonitoringSession
  ├── engine.policy_engine.PolicyEngine                    [ACTIVE]
  │     └── policy.evaluator.PolicyEvaluator               [ACTIVE]
  │           └── policy.classifier.* (app_matches, domain_matches, normalize_domain)  [ACTIVE]
  ├── engine.action_engine.ActionEngine                    [ACTIVE]
  │     └── adapters.mock.action_executor.MockActionExecutor   [ACTIVE] (canonical executor)
  ├── engine.escalation_engine.EscalationEngine            [ACTIVE]
  ├── persistence.repository.InMemoryActivityRepository    [ACTIVE]
  ├── services.screenshot_service.ScreenshotService        [ACTIVE]
  │     └── platform.screen.get_screen_provider            [ACTIVE factory]
  │           ├── platform.windows_screen.WindowsScreenProvider  [ACTIVE on Windows] (backend=mss)
  │           └── platform.mock_screen.MockScreenProvider       [ACTIVE on non-Windows]
  └── logging                                             [ACTIVE]
        ├── logging.logger (configure_logging, get_logger, set_log_context, StructuredFormatter)
        ├── logging.context
        └── logging.sanitize
```

---

## 4. Server Graph (E5)

```
server.py app (FastAPI)
  ├── engine.facade.ActivityEngine
  ├── services.browser_state.BrowserStateStore     [ACTIVE]
  │     └── services.browser_events.normalize_extension_event  [ACTIVE]
  ├── transport.dto (MessageType, WireMessage)     [ACTIVE]
  ├── transport.protocol                           [ACTIVE]
  ├── transport.serialization                      [ACTIVE]
  └── utils.ids.short_id                           [ACTIVE]

Endpoints implemented:
  GET  /            health
  POST /api/session/start | /api/session/end
  GET  /api/state | /api/metrics | /api/events | /api/session/status
  POST /api/telemetry
  POST /api/browser/telemetry       ← consumed by browser extension (E6)
  GET  /api/browser/active          ← polled by ExtensionBrowserMonitor
  POST /api/screenshot
  WS   /ws
```

---

## 5. Browser Extension Graph (E6)

```
background.js
  ├── manifest.json (MV3, permissions: tabs; host: localhost:8000)
  ├── config.json (api_base_url, device_id, max_queue, retry…)
  ├── chrome.tabs.* listeners → build payload (kind=browser_navigation/browser_tab_focus/browser_tab_close)
  └── POST {api_base_url}/api/browser/telemetry   ← → server (E5)
```

**Extension ↔ Server contract:** [ACTIVE + MATCH]
- Extension sends `{ events: [ {kind, source, timestamp, device_id, tab_id, domain, url, title} ] }`
- Server model `BrowserTelemetryPayload.events: list[dict[str, Any]]` — accepts the batched payload. ✅

---

## 6. Resource / Static Files

| File | Consumers | Status |
|------|-----------|--------|
| `src/activity_engine/policy/default_policies.yaml` | `policy.loader` (importlib.resources) | **[ACTIVE]** |
| `examples/policy.yaml` | `examples/`, docs; NOT the running default | **[ACTIVE/DEMO]** |
| `src/assets/hero.png` | none (no React build entry) | **[ORPHAN]** (phantom frontend) |
| `src/assets/react.svg`, `src/assets/vite.svg` | none | **[ORPHAN]** (phantom frontend) |
| `public/favicon.svg`, `public/icons.svg` | `index.html` (static scaffold only) | **[ORPHAN/UNUSED]** runtime |
| `index.html`, `vite.config.js`, `package.json`, `package-lock.json` | frontend scaffold; **NO React source entry** (no `src/main.*`, no `*.jsx/tsx`) | **[BROKEN]** — build has no entry |
| `Flowsink/` | none | **[EMPTY]** directory (not a legacy system) |
| `src/student_activity_engine.egg-info/` | build artifact (stale) | **[LEGACY/ARTIFACT]** |

---

## 7. Unreachable / Orphan Nodes

| Module | Evidence | Classification |
|--------|----------|----------------|
| `services/activity_service.py` | 0 imports/references anywhere; only docstring mention in state_engine.py | **[ORPHAN]** |
| `services/bedtime_service.py` | 0 imports/references | **[ORPHAN]** |
| `services/focus_service.py` | 0 imports/references | **[ORPHAN]** |
| `utils/logging.py` | "backward-compatible delegate"; not imported by any module | **[LEGACY/DEAD]** |
| `adapters/windows/browser_monitor.py` | never imported/constructed; cli uses MockBrowserMonitor even on Windows `real` backend | **[ORPHAN]** |
| `adapters/windows/action_executor.py` | never imported; `raise NotImplementedError("future extension")` | **[ORPHAN/LEGACY]** stub |
| `src/assets/*` | no runnable frontend consumes them | **[ORPHAN]** |
| `public/favicon.svg`, `public/icons.svg` | only static scaffold | **[ORPHAN/UNUSED]** |
| `DATABASE_ARCHITECTURE_AND_FLOW_REPORT.docx` | not referenced by code; doc-only | **[DOC]** (keep) |

---

## 8. Broken References

| Source | Target | Type | Status |
|--------|--------|------|--------|
| `cli/main.py:_cmd_policy_check` (default `config/policy.yaml`) | `config/policy.yaml` | file | **[BROKEN]** — `config/` does not exist |
| `config.loader.load_dev_config` | `config/local.yaml` | file | **[BROKEN]** optional — graceful fallback exists |
| `index.html` / `vite.config.js` | React entry (`src/main.*`, `App.*`) | frontend | **[BROKEN]** — no entry file; `vite build` fails |

---

## 9. Legacy Detection

| Concern | Current/Canonical | Legacy/Old | Runtime uses |
|---------|-------------------|-----------|--------------|
| Logging | `logging/` package | `utils/logging.py` (delegate) | `logging/` only; `utils/logging.py` unused |
| Action executor | `adapters/mock/action_executor.py` | `adapters/windows/action_executor.py` (NotImplementedError) | mock always (CLI + facade default) |
| Screenshot | `platform/screen.py` factory | none (mock/windows both are runtime platforms) | factory dispatch; both screens active by platform |
| Frontend | (none implemented) | `index.html`+vite scaffold only | phantom — no runtime |

---

## 10. Full Node List (src/activity_engine)

All `.py` under `src/activity_engine/` are **[ACTIVE]** via the CLI or server reachability EXCEPT the orphans listed in §7. Detailed per-file import graph is in `FULL_SYSTEM_COMPATIBILITY_AUDIT.md`.